# Single source of truth for dev/test dependencies — what `make setup` and CI install
# (`pip install -r requirements-dev.txt`). Dependabot updates this file directly, so the
# declared floors match what CI actually resolves. Runtime deps live in requirements.txt.
-r requirements.txt
pytest>=9.1.1
pytest-asyncio>=1.4.0
pytest-cov>=7.1.0
ruff>=0.15.20
mypy>=2.1.0
types-PyYAML>=6.0.12.20260518
hypothesis>=6.156.6
pre-commit>=4.6.0
